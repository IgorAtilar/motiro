import { Theme as ThemeType } from '@react-navigation/native';
import { Colors } from './Colors';

export const Theme: ThemeType = {
  dark: false,
  colors: {
    primary: Colors.beta,
    background: Colors.delta,
    card: Colors.epsilon,
    text: Colors.gamma,
    border: Colors.eta,
    notification: Colors.eta,
  },
};
